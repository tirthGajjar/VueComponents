import { Keys } from '@/keyboard'

export const TAG_SELECTION_MODE = {
  SINGLE: 'single',
  MULTIPLE: 'multiple',
}

export const keysThatRequireToOpenMenu = [
  Keys.Enter,
  Keys.ArrowUp,
  Keys.ArrowDown,
  Keys.Space,
]
